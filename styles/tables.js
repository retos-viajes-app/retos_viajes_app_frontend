import {StyleSheet} from 'react-native';
const tablesStyles = StyleSheet.create({
    row:{
        flexDirection:'row',
    },
    cell:{
        padding:8,
        flexDirection:'column',
        gap: 10,
        flex:1
    }
});
export default tablesStyles;